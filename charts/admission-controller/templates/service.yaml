apiVersion: v1
kind: Service
metadata:
  labels:
    role: {{ include "helios.fullname" . }}
  name: {{ include "helios.fullname" . }}
spec:
  ports:
    - port: 443
      targetPort: 8443
  selector:
    app: {{ include "helios.fullname" . }}